<%= @patient.name %>

<ul>
  <% @patient.doctors.each do |doctor| %>
  <li><%= link_to doctor.name, doctor_path(doctor) %></li>
  <% end %>
</ul>

<ul>
  <% @patient.appointments.each do |appt| %>
    <li><%= appt.convert_datetime(appt.appointment_datetime) %></li>
  <% end %>
</ul>
